---
title: 'Development'
description: 'Learn how to preview changes locally'
---

We welcome contributions from developers and security researchers of all skill levels. Whether you want to report a bug, suggest a new feature, improve the documentation, or write code, your contributions are invaluable. Here’s how you can get started:

<Info>
  **Prerequisite** You should have installed Node.js (version 18.10.0 or
  higher).
</Info>

Step 1. Clone the BLOCKEF repository:

<CodeGroup>

```bash npm
git clone https://github.com/blockdef/web3-security-testing-guide.git
```

</CodeGroup>

Or using GitHub CLI:

```bash
gh repo clone blockdef/web3-security-testing-guide
```


Step 2. Installing dependencies once in the project's root directory, run the following command to install the project's dependencies:

```bash pnpm
pnpm install
```

Step 3. After installing the dependencies once in the project's root directory, run the following command to start the project:

```bash pnpm
pnpm run dev
```

### Custom Ports

BLOCKDEF uses port 3000 by default. You can use the `--port` flag to customize the port the repository runs on.


## Troubleshooting

Here's how to solve some common problems when working with the repository.

<AccordionGroup>
  <Accordion title="BLOCKDEF is not loading">
    Update to Node v18. Run `npm install` and try again.
  </Accordion>
  <Accordion title="No such file or directory on Windows">
Go to the `C:/Users/Username/.blockdef/` directory and remove the `mint`
folder. Then Open the Git Bash in this location and run `git clone
https://github.com/blockdef`.

Repeat step 3.

  </Accordion>
  <Accordion title="Getting an unknown error">
    Try navigating to the root of your device and delete the ~/.BLOCKDEF folder.
    Then run `BLOCKDEF dev` again.
  </Accordion>
</AccordionGroup>

Curious about what changed in a CLI version? [Check out the CLI changelog.](/changelog/command-line)


## Documentation 

The documentation website is now available at:

<CodeGroup>

```bash npm
git clone https://github.com/blockdef/docs.git
```

</CodeGroup>